# hand_labelled — the trusted keystone (to grow)

Small, hand-authored, **real-string** eval set with labels we trust: real distribution,
our gold, varying taxonomies, immune to training-data leakage (private). The set you'd
ultimately *decide* on.

Today it's ~12 placeholder example records (heavy on the tail, one description-flip pair) —
we're using `llm_generated` for now. **Grow this** with real, anonymized transactions you
label by hand, weighted toward the cryptic long tail and toward taxonomies real users bring.
