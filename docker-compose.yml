services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: transclassify
      POSTGRES_PASSWORD: transclassify
      POSTGRES_DB: transclassify
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U transclassify"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
